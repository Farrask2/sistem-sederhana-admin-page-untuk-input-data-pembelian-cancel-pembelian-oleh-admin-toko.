<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Stok - Laptop Store</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="container dashboard-container">
    <div class="header-section text-center mb-5">
        <h2 class="text-primary fw-bold"><i class="fa-solid fa-warehouse me-2"></i>Status Gudang</h2>
        <div class="d-inline-flex bg-white p-1 rounded-pill shadow-sm border mt-3">
            <a href="/" class="btn btn-white rounded-pill px-4 text-muted">Input</a>
            <a href="/riwayat" class="btn btn-white rounded-pill px-4 text-muted">Riwayat</a>
            <a href="/stok" class="btn btn-primary rounded-pill px-4 shadow-none">Stok</a>
        </div>
    </div>

    <div class="row g-4">
        <% produk.forEach(p => { %>
            <div class="col-md-4">
                <div class="glass-card h-100 border-top border-4 <%= p.stok <= 5 ? 'border-danger' : 'border-primary' %>">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <h6 class="fw-bold mb-0"><%= p.nama %></h6>
                        <span class="badge <%= p.stok <= 5 ? 'bg-danger' : 'bg-primary' %>"><%= p.stok %> Unit</span>
                    </div>
                    <p class="text-muted small mb-3">Harga: Rp<%= p.harga.toLocaleString('id-ID') %></p>
                    
                    <form action="/restock/<%= p.id %>" method="POST" class="input-group input-group-sm">
                        <input type="number" name="tambah_stok" class="form-control shadow-none" placeholder="Tambah unit..." min="1" required>
                        <button class="btn btn-outline-success" type="submit"><i class="fa-solid fa-plus"></i></button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
</div>
</body>
</html>